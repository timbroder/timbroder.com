---
interface Props {
  padded?: boolean
}

const { padded = true } = Astro.props
---

<h1 class:list={[padded && 'mb-6', 'text-4xl font-bold tracking-tight text-zinc-800 sm:text-5xl']}>
  <slot />
</h1>
